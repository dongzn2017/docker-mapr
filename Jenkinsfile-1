pipeline {
  agent none
  stages {
    stage('Pre'){
      agent none
      steps{
       sh 'sudo chmod 666 /var/run/docker.sock'
      }
    }
    stage('Test') {
      agent {
        docker { image 'ubuntu' }
            }
      steps {
        sh 'ls -a'
      }
    }
  }
}
